name: release

on: workflow_dispatch

jobs:
  release:
    permissions:
      contents: write
      packages: write
    uses: dbstar-org/general/.github/workflows/maven-release.yml@main
    secrets: inherit